## CodeceptJS with Protractor

Meta-package which installs [CodeceptJS](http://codecept.io) with Protractor.

```
npm i codeceptjs-protractor
```

Run codeceptjs:

```
./node_modules/.bin/codeceptjs init
```

And choose `Protractor` helper from the list.

